import { StyleSheet } from 'react-native';

const contentDetailsStyle = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    margin: 10,
    textAlign: 'center',
  },
  webView: {
    flex: 1,
  },
});

export default contentDetailsStyle;
